apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: master-data-db-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: master-data-db
  labels:
    app: master-data-db
spec:
  replicas: 1
  selector:
    matchLabels:
      app: master-data-db
  template:
    metadata:
      labels:
        app: master-data-db
    spec:
      containers:
        - name: postgres
          image: postgres:16-alpine
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: master-data-service-db
                  key: username
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: master-data-service-db
                  key: password
            - name: POSTGRES_DB
              value: master_data_db
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
          livenessProbe:
            exec:
              command: ["pg_isready", "-U", "postgres"]
            initialDelaySeconds: 10
            periodSeconds: 10
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: master-data-db-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: master-data-db
  labels:
    app: master-data-db
spec:
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: 5432
      name: postgres
  selector:
    app: master-data-db
